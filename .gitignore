# Virtual environment
venv/
.venv/
env/

# Environment variables
.env

# Python caching
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/

# OS files
.DS_Store
Thumbs.db

# Project outputs
*.zip
*.log
test_output.txt

# Cached raw PubMed pulls, chunk exports, and query logs (regenerable via
# src.ingest.run_ingest) — but keep the corpus freeze manifest (provenance
# record for whatever corpus is currently in Pinecone) and evaluation
# artifacts that go in the paper (threshold sweep, eval run results).
data/*
!data/corpus_manifest.json
!data/threshold_sweep.csv
!data/eval_results.csv
